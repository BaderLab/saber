language: python

os:
  - linux
  - osx

matrix:
  allow_failures:
    - os: osx
    - python: "3.5"

python:
  - "3.6"
  - "3.5"

install: pip install tox pytest-cov coveralls

script: tox

after_success: coveralls
